# Open API Specs Translation Example

## Run the server:

```sh
iex -S mix phx.server
```

The translated JSON can be accessed at [http://localhost:4000/ja/openapi](http://localhost:4000/ja/openapi).
